node_modules
.pnpm-store
*.pgsql
*.log
.env
.DS_Store
/node_modules/

# React Router
/.react-router/
/build/

# Claude Code agent worktrees (transient)
.claude/worktrees/

# tracker (implementation tracking lives in the workspace root, not here)
.tracker/

# Claude Code agent session/memory notes (local, not for public repo)
.claude/agent-memory/

# screenshot-tool artifact from a mistyped --full-page flag
/--full-page
